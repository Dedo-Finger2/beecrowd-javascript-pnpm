/**
 * @param { Array<unknown> } lines - Variable with all data provided in order
 * @returns { string }
 */
export function challenge1000(lines) {
  const mesage = "Hello World!";
  console.log(mesage);
  return mesage;
}
